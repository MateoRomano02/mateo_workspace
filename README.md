# mateo_workspace

Workspace personal para construir el sistema de contexto multi-repo.

## Apps

- `app/`: frontend base (React + TypeScript + Vite + Tailwind v4)

## Quick start

- `npm run dev` (from repo root) to run the app in `app/`
- `npm run lint`
- `npm run typecheck`
